import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF7C3AED);
  static const accent = Color(0xFFFBBF24);
  static const bgLight = Color(
    0xFFEDE7F6,
  ); // Lavender-grey background for white cards
  static const textDark = Color(0xFF1E1E1E);
}
