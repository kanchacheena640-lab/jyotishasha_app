name: jyotishasha_app
description: "Jyotishasha Astrology App"
publish_to: 'none' # app not published on pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # 🌐 HTTP Requests
  http: ^1.2.2

  # 🔥 Firebase Core + Auth + Firestore
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.3

  # 🔑 Google & Facebook Sign-In
  google_sign_in: ^6.2.1
  flutter_facebook_auth: ^6.1.1

  # 🗓️ Date & Time Formatting
  intl: ^0.19.0

  # 💎 UI & Icons
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # 📦 Assets
  assets:
    - assets/icons/google.png
    - assets/icons/facebook.png
